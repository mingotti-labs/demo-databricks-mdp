[project]
name = "demo-databricks-mdp"
version = "0.1.0"
description = "Databricks demo platform: ingestion, modeling, Genie, ML, GenAI, and Apps"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
testpaths = ["tests/common"]

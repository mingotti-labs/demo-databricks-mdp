resources:
  pipelines:
    clickstream_autoloader_sql_add_new_columns:
      name: bronze--clickstream--autoloader--sql--add-new-columns--${bundle.target}
      catalog: ${var.catalog}
      schema: bronze_clickstream
      serverless: true
      continuous: false
      configuration:
        clickstream_catalog: ${var.catalog}
      libraries:
        - glob:
            include: ../../src/layers/bronze/clickstream/sql_add_new_columns/**

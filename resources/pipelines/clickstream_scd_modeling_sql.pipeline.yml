resources:
  pipelines:
    clickstream_scd_modeling_sql:
      name: bronze--clickstream--scd--sql--${bundle.target}
      catalog: ${var.catalog}
      schema: bronze_clickstream_publish
      serverless: true
      continuous: false
      libraries:
        - glob:
            include: ../../src/layers/bronze/clickstream_publish/sql/**

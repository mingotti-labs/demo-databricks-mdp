resources:
  pipelines:
    silver_landing_geonames:
      name: silver--landing--geonames--${bundle.target}
      catalog: ${var.catalog}
      schema: silver_landing_geonames
      serverless: true
      continuous: false
      configuration:
        workspace_file_path: ${workspace.file_path}
      libraries:
        - glob:
            include: ../../src/layers/silver/landing/geonames/**
        - glob:
            include: ../../src/common/**

resources:
  pipelines:
    clickstream_autoloader_sql_none:
      name: bronze--clickstream--autoloader--sql--none--${bundle.target}
      catalog: ${var.catalog}
      schema: bronze_clickstream
      serverless: true
      continuous: false
      configuration:
        clickstream_catalog: ${var.catalog}
      libraries:
        - glob:
            include: ../../src/layers/bronze/clickstream/sql_none/**

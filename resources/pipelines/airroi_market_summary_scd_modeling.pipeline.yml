resources:
  pipelines:
    airroi_market_summary_scd_modeling:
      name: bronze--airroi--scd--${bundle.target}
      catalog: ${var.catalog}
      schema: bronze_airroi_publish
      serverless: true
      continuous: false
      libraries:
        - glob:
            include: ../../src/layers/bronze/airroi_publish/**

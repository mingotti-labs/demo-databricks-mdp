resources:
  pipelines:
    clickstream_autoloader_python_rescue:
      name: bronze--clickstream--autoloader--python--rescue--${bundle.target}
      catalog: ${var.catalog}
      schema: bronze_clickstream
      serverless: true
      continuous: false
      configuration:
        clickstream_catalog: ${var.catalog}
      libraries:
        - glob:
            include: ../../src/layers/bronze/clickstream/python_rescue/**

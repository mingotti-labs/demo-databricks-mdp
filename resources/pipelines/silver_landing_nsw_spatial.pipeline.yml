resources:
  pipelines:
    silver_landing_nsw_spatial:
      name: silver--landing--nsw_spatial--${bundle.target}
      catalog: ${var.catalog}
      schema: silver_landing_nsw_spatial
      serverless: true
      continuous: false
      configuration:
        workspace_file_path: ${workspace.file_path}
      libraries:
        - glob:
            include: ../../src/layers/silver/landing/nsw_spatial/**
        - glob:
            include: ../../src/common/**

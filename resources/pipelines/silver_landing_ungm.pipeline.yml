resources:
  pipelines:
    silver_landing_ungm:
      name: silver--landing--ungm--${bundle.target}
      catalog: ${var.catalog}
      schema: silver_landing_ungm
      serverless: true
      continuous: false
      configuration:
        workspace_file_path: ${workspace.file_path}
      libraries:
        - glob:
            include: ../../src/layers/silver/landing/ungm/**
        - glob:
            include: ../../src/common/**

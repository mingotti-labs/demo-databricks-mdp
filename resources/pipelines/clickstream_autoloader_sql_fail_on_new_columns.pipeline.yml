resources:
  pipelines:
    clickstream_autoloader_sql_fail_on_new_columns:
      name: bronze--clickstream--autoloader--sql--fail-on-new-columns--${bundle.target}
      catalog: ${var.catalog}
      schema: bronze_clickstream
      serverless: true
      continuous: false
      configuration:
        clickstream_catalog: ${var.catalog}
      libraries:
        - glob:
            include: ../../src/layers/bronze/clickstream/sql_fail_on_new_columns/**

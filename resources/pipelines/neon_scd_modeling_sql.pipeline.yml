resources:
  pipelines:
    neon_scd_modeling_sql:
      name: bronze--neon--scd--sql--${bundle.target}
      catalog: ${var.catalog}
      schema: bronze_neon_publish
      serverless: true
      continuous: false
      libraries:
        - glob:
            include: ../../src/layers/bronze/neon_publish/sql/**

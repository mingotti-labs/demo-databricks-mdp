resources:
  pipelines:
    iso_country_reference_scd_modeling:
      name: bronze--iso--country-reference-scd--${bundle.target}
      catalog: ${var.catalog}
      schema: bronze_iso_publish
      serverless: true
      continuous: false
      libraries:
        - glob:
            include: ../../src/layers/bronze/iso_publish/**

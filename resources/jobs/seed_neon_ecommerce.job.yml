resources:
  jobs:
    seed_neon_ecommerce:
      name: seed--neon--data--${bundle.target}
      tasks:
        - task_key: seed
          notebook_task:
            notebook_path: ../../src/seed_data/seed_neon_ecommerce.py
          environment_key: default
      environments:
        - environment_key: default
          spec:
            environment_version: "2"
            dependencies:
              - psycopg2-binary
              - faker

resources:
  jobs:
    generate_clickstream_events:
      name: generate--clickstream--events--${bundle.target}
      tasks:
        - task_key: generate
          notebook_task:
            notebook_path: ../../src/seed_data/generate_clickstream_events.py
            base_parameters:
              catalog: ${var.catalog}
          environment_key: default
      environments:
        - environment_key: default
          spec:
            environment_version: "2"
            dependencies:
              - faker
